#!/bin/sh

while true; do
    input=$(dmenu < /dev/null -p "$input" -fn "Source Code Variable:pixelsize=19:antialias=true:autohint=true" | sed "s/@/$answer/g" | xargs -rI {} qalc "{}")
    answer=$(echo $input | sed "s/.* = //")
    [ -z $input ] && exit
done
