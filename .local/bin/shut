#!/bin/bash

what=$(echo -e "shutdown\nreboot" | dmenu -fn "mononoki nerd font:pixelsize=19:antialias=true:autohint=true")

[ $what = "shutdown" ] && prompt "Shutdown system?" "systemctl poweroff"
[ $what = "reboot" ] && prompt "Reboot system?" "systemctl reboot"
